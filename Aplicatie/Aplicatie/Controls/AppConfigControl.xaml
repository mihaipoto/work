<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Aplicatie.Controls.AppConfigControl"
             xmlns:controls="clr-namespace:Aplicatie.Controls"
             xmlns:viewmodel="clr-namespace:Aplicatie.ViewModels"
             x:DataType="viewmodel:AppConfigVM">


    <VerticalStackLayout Spacing="5">
        <!--<controls:GeneralConfiguration BindingContext="{Binding GeneralConfiguration}" />-->

        <Label Text="General Settings" />
        <controls:GeneralSettingsControl BindingContext="{Binding GeneralConfiguration}" />
        <!--<CollectionView ItemsSource="{Binding PossibleOperatingModes}"
                        SelectedItem="{Binding CurrentOperatingMode}"
                        ItemsLayout="HorizontalList"
                        RadioButtonGroup.GroupName="ttt">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout>
                        <RadioButton Value="{Binding }"
                                     Content="{Binding }"
                                      />
                    </HorizontalStackLayout>

                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>-->

    </VerticalStackLayout>
</ContentView>
