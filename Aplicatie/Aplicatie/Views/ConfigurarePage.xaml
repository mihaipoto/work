<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Aplicatie.Pages.ConfigurarePage"
             xmlns:viewModels="clr-namespace:Aplicatie.ViewModels"
             x:DataType="viewModels:ConfigurareViewModel"
             Title="ConfigurarePage">
    <VerticalStackLayout>
        
            <Button Text="{Binding CurrentTheme, Converter={StaticResource WhiteOrDarKThemeBindingConverter}}"
                        Command="{Binding ToggleDarkLightCommand}"
                        HorizontalOptions="EndAndExpand"
                        VerticalOptions="EndAndExpand" />
    </VerticalStackLayout>
</ContentPage>